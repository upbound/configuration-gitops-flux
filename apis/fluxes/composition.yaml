apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: fluxes.gitops.platform.upbound.io
spec:
  compositeTypeRef:
    apiVersion: gitops.platform.upbound.io/v1alpha1
    kind: Flux
  mode: Pipeline
  pipeline:
  - functionRef:
      name: upbound-configuration-gitops-fluxflux
    step: xflux
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
